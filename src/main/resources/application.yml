amazon:
    access:
        key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}
    region: us-east-1
    end-point:
        url: dynamodb.us-east-1.amazonaws.com
server:
    port: 8080